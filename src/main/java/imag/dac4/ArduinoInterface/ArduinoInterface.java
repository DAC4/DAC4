package imag.dac4.ArduinoInterface;

/*

public class ArduinoInterface {

    private Link link;
    private Lockers lockers;

    public boolean initialisation() {

        Link link = Link.getDefaultInstance();
        boolean connected = link.connect("/dev/ttyACM0", 115200);
        Lockers lockers = new Lockers(link);

        return connected;
    }

    public boolean openBox(int numBox){
        lockers.getArduinoBoxManager(numBox).openBox();
    }

    public boolean isThereProduct(int numBox){
       lockers.getArduinoBoxManager(numBox).isThereProduct();
    }

    public boolean isDoorOpen(int numBox){
               lockers.getArduinoBoxManager(numBox).isDoorOpen();
    }

    public boolean changeState(String state, int numBox){
       lockers.getArduinoBoxManager(numBox).changeState(state);
    }

    public boolean closeDoor(int numBox){
        lockers.getArduinoBoxManager(numBox).closeDoor();
    }

    public boolean insertProduct(int numBox) {
               lockers.getArduinoBoxManager(numBox).insertProduct();
    }

    public boolean removeProduct(int numBox) {
               lockers.getArduinoBoxManager(numBox).removeProduct();
    }
*/
